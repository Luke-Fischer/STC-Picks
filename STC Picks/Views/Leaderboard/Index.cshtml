@{
    ViewData["Title"] = "Leaderboard";
}
﻿
@model IEnumerable<User>

<br />
<table class="table" style="background-color:white; color:white">
    <thead style="background-color:#D06B28;">
        <tr style="color:white;">
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Score</th>
        </tr>
    </thead>
    <tbody>
        @{
            int place = 1;
            int displayPlace = 0;
            int currentScore = -1;
            foreach(var obj in Model){
                string dispName = obj.fullName;
                if(obj.score != currentScore)
                {
                    displayPlace = place;
                }
                <tr>
                    <th style="color:#D06B28;" scope="row">@displayPlace</th>
                    <td><strong><a style="text-decoration:none; color:#D06B28;" asp-controller="Leaderboard" asp-action="ViewPicks" asp-route-id="@obj.fullName">@dispName</a></strong></td>
                    <td style="color:#D06B28;">@obj.score</td>
                </tr>
                currentScore = obj.score;
                place++;
            }
        }
    </tbody>
</table>